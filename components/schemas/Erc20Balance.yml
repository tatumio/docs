type: object
properties:
  balance:
    type: string
    description: The number of fungible tokens in the smallest token unit (for example,
      if the token has 10 decimal places, the number is returned as 9*10^10)
    example: '10'
