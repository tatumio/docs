properties:
  errorCode:
    description: trade.currency1AccountId.customer.disabled
    example: trade.currency1AccountId.customer.disabled
    type: string
  message:
    description: Currency1 account's customer ${currency1Account.customerId} is disabled,
      unable to perform exchange trade.
    example: Currency1 account's customer ${currency1Account.customerId} is disabled,
      unable to perform exchange trade.
    type: string
  statusCode:
    description: '403'
    example: 403
    type: number
required:
- errorCode
- message
- statusCode
type: object
