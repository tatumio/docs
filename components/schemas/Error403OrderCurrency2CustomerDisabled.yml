properties:
  errorCode:
    description: trade.currency2AccountId.customer.disabled
    example: trade.currency2AccountId.customer.disabled
    type: string
  message:
    description: Currency2 account's customer ${currency2Account.customerId} is disabled,
      unable to perform exchange trade.
    example: Currency2 account's customer ${currency2Account.customerId} is disabled,
      unable to perform exchange trade.
    type: string
  statusCode:
    description: '403'
    example: 403
    type: number
required:
- errorCode
- message
- statusCode
type: object
