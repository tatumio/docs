properties:
  errorCode:
    description: trade.currency2AccountId.frozen
    example: trade.currency2AccountId.frozen
    type: string
  message:
    description: Currency2 account ${trade.currency2AccountId} is frozen, unable to
      perform trade.
    example: Currency2 account ${trade.currency2AccountId} is frozen, unable to perform
      trade.
    type: string
  statusCode:
    description: '403'
    example: 403
    type: number
required:
- errorCode
- message
- statusCode
type: object
