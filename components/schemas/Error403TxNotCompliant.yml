properties:
  errorCode:
    description: transaction.not.compliant
    example: transaction.not.compliant
    type: string
  message:
    description: Transaction not compliant, recipient account customer country is
      not defined.
    example: Transaction not compliant, recipient account customer country is not
      defined.
    type: string
  statusCode:
    description: '403'
    example: 403
    type: number
required:
- errorCode
- message
- statusCode
type: object
