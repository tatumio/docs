properties:
  errorCode:
    description: senderAccount.customer.disabled
    example: senderAccount.customer.disabled
    type: string
  message:
    description: Sender accounts customer ${senderAccount.customerId} is disabled,
      unable to perform payment.
    example: Sender accounts customer ${senderAccount.customerId} is disabled, unable
      to perform payment.
    type: string
  statusCode:
    description: '403'
    example: 403
    type: number
required:
- errorCode
- message
- statusCode
type: object
