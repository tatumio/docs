properties:
  errorCode:
    description: senderAccount.frozen
    example: senderAccount.frozen
    type: string
  message:
    description: Sender account ${transaction.senderAccountId} is frozen, unable to
      perform payment.
    example: Sender account ${transaction.senderAccountId} is frozen, unable to perform
      payment.
    type: string
  statusCode:
    description: '403'
    example: 403
    type: number
required:
- errorCode
- message
- statusCode
type: object
