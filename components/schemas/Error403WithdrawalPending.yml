properties:
  errorCode:
    description: withdrawal.pending
    example: withdrawal.pending
    type: string
  message:
    description: Unable to prepare withdrawal, last withdrawal with id ${id} is not
      yet processed by blockchain.
    example: Unable to prepare withdrawal, last withdrawal with id ${id} is not yet
      processed by blockchain.
    type: string
  statusCode:
    description: '403'
    example: 403
    type: number
required:
- errorCode
- message
- statusCode
type: object
